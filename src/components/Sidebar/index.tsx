const Sidebar = () => {
  return (
    <aside
      className={
        "h-full flex bg-card fixed top-0 right-0 overflow-hidden text-nowrap font-sans text-lg  z-10 flex-col border-l transition-all duration-300 ease-in-out w-[350px] p-4 -md:hidden"
      }
    >
      Sidebar
    </aside>
  );
};

export default Sidebar;
